version: '3'
services:
  server:
    image: billboard-server
    restart: always
    ports:
      - '9000:9000'
    build: 
      context: ./server
      dockerfile: Dockerfile
    volumes:
      - ./server:/server
      - /server/node_modules
    depends_on:
      - mongo
    networks: 
      - billboardnetwork
  client:
    image: billboard-client
    restart: always
    ports:
      - '3000:3000'
    build: 
      context: ./client
      dockerfile: Dockerfile
    volumes:
      - ./client:/client 
      - /client/node_modules
    links:
      - server
    networks: 
      - billboardnetwork
  mongo:
    image: mongo
    restart: always
    container_name: mongo
    ports:
      - '27017:27017'
    networks: 
      - billboardnetwork
networks:
  billboardnetwork:
    driver: bridge